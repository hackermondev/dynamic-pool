# Dynamic Pool
Modified version of [dynamic-pool](https://github.com/discord/dynamic-pool) with support for mapped pools and expiring objects. 

# Install
```toml
[dependencies]
dynamic-pool = { git = "https://github.com/hackermondev/dynamic-pool" }
```